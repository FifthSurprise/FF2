<h2>Stacks currently being studied: </h2>
<ol>
  <% @user_stacks.each do |ustack| %>
  <li><h2><%= link_to ustack.stack.name, user_stack_path(ustack) %></h2></li>
  <% end %>
</ol>

<% if @user.user_stacks.size == 0 %>
<h3> You have no stacks to study. :( </h3>
<% end %>

</br>
<%= link_to 'Get more stacks', stacks_path, :class => "btn btn-default btn-md"%>
</br>