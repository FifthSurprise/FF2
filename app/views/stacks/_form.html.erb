<%= form_for(@stack) do |f| %>
<% if @stack.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@stack.errors.count, "error") %> prohibited this stack from being saved:</h2>
</div>
<% end %>

<div class ="field">
  <%= f.label :name %> </br>
  <%= f.text_area :name, :size => "80x10" %>
</div>
<div class ="field">
  <%= f.label :description %></br>
  <%= f.text_area :description, :size => "80x10" %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>